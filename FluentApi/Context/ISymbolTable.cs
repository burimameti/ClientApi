using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
//using IntegrationTests.Builder.Entries;
//using Representations.Cofamilies;

namespace Cofamilies.Client.FluentApi.Context
{
  public interface ISymbolTable
  {
    /*
    T GetCurrent<T>() where T : class, IEntry;
    void MakeCurrent<T>(T entry) where T : class, IEntry;
    void MakeCurrent<T>(string label) where T : class, IEntry;

    FeedEntry<T> CurrentFeed<T>() where T : class, IRestRepresentation;
    //void CurrentFeed<T>(FeedEntry<T> feedEntry) where T : class, IRestRepresentation;

    IEntry  GetEntry(params Type[] types);
    T GetEntry<T>(string label = null) where T : class, IEntry;
    T GetEntry<T>(int n) where T : class, IEntry;
    IEntry GetMostRecentEntryWithLinkRelationship(string rel);
    void SaveAs<T>(string label, T entry) where T : class, IEntry;

    FeedEntry<T> GetFeed<T>(string label) where T : class, IRestRepresentation;
    void Feed<T>(string label, FeedEntry<T> feed) where T : class, IRestRepresentation;

    void RemoveEntry<T>(int n) where T : class, IEntry;
     */

  }
}
